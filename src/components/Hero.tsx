import { motion } from 'framer-motion';\nimport { siteConfig } from '../config/site.config';\n\nconst Hero = () => {\n  return (\n    <section id='hero' className='relative h-screen overflow-hidden'>\n      <video \n        autoPlay \n        loop \n        muted \n        className='absolute top-0 left-0 w-full h-full object-cover'\n      >\n        <source src='/videos/luxury-background.mp4' type='video/mp4' />\n      </video>\n\n      <div className='absolute inset-0 bg-black/50 flex items-center justify-center text-center p-4'>\n        <motion.div\n          initial={{ opacity: 0, y: 50 }}\n          animate={{ opacity: 1, y: 0 }}\n          transition={{ duration: 1.2, delay: 0.5 }}\n        >\n          <h2 className='text-5xl md:text-7xl font-bold text-white mb-4'>{siteConfig.tagline}</h2>\n          <p className='text-xl text-gray-300 max-w-2xl mx-auto'>{siteConfig.products[0].description}</p>\n        </motion.div>\n      </div>\n    </section>\n  );\n};\n\nexport default Hero;