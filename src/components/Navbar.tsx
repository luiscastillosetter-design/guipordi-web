import { motion } from 'framer-motion';\nimport { siteConfig } from '../config/site.config';\n\nconst Navbar = () => {\n  return (\n    <motion.nav \n      initial={{ y: -100 }}\n      animate={{ y: 0 }}\n      transition={{ duration: 0.8 }}\n      className='fixed top-0 w-full z-50 backdrop-blur-md bg-black/30 p-4'\n    >\n      <div className='container mx-auto flex justify-between items-center'>\n        <h1 className='text-2xl font-bold text-white'>{siteConfig.brandName}</h1>\n        <div className='flex gap-8'>\n          <a href='#hero' className='text-white hover:text-gold transition'>Home</a>\n          <a href='#products' className='text-white hover:text-gold transition'>Products</a>\n          <a href='#contact' className='text-white hover:text-gold transition'>Contact</a>\n        </div>\n      </div>\n    </motion.nav>\n  );\n};\n\nexport default Navbar;