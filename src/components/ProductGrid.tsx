import { motion } from 'framer-motion';\nimport { siteConfig } from '../config/site.config';\n\nconst ProductGrid = () => {\n  return (\n    <section id='products' className='py-20 bg-black'>\n      <div className='container mx-auto px-4'>\n        <motion.h2\n          initial={{ opacity: 0 }}\n          whileInView={{ opacity: 1 }}\n          viewport={{ once: true }}\n          className='text-4xl font-bold text-white text-center mb-12'\n        >\n          Exclusive Collections\n        </motion.h2>\n\n        <div className='grid grid-cols-1 md:grid-cols-2 gap-8'>\n          {siteConfig.products.map((product) => (\n            <motion.div\n              key={product.id}\n              whileHover={{ y: -10 }}\n              transition={{ duration: 0.3 }}\n              className='bg-gray-900 rounded-lg overflow-hidden shadow-lg'\n            >\n              <img \n                src={product.image} \n                alt={product.name} \n                className='w-full h-64 object-cover' \n              />\n              <div className='p-6'>\n                <h3 className='text-2xl text-white font-semibold mb-2'>{product.name}</h3>\n                <p className='text-gray-400 mb-4'>{product.description}</p>\n                <div className='flex justify-between items-center'>\n                  <span className='text-gold text-xl font-bold'>{product.price}</span>\n                  <button className='bg-gold text-black px-6 py-2 rounded-full hover:bg-gold-dark transition'>Buy Now</button>\n                </div>\n              </div>\n            </motion.div>\n          ))}\n        </div>\n      </div>\n    </section>\n  );\n};\n\nexport default ProductGrid;