import { motion } from 'framer-motion';\nimport { siteConfig } from '../config/site.config';\n\nconst ServicesGrid = () => {\n  return (\n    <section id='services' className='py-24 bg-gray-950'>\n      <div className='container mx-auto px-4'>\n        <motion.h2\n          initial={{ opacity: 0, y: 50 }}\n          whileInView={{ opacity: 1, y: 0 }}\n          viewport={{ once: true }}\n          className='text-5xl font-bold text-white text-center mb-20'\n        >\n          Bespoke Services\n        </motion.h2>\n\n        <div className='grid grid-cols-1 md:grid-cols-3 gap-8'>\n          {siteConfig.services.map((service, index) => (\n            <motion.div\n              key={index}\n              whileHover={{ scale: 1.05 }}\n              className='bg-black p-8 rounded-2xl text-center'\n            >\n              <div className='text-4xl mb-4 text-amber-400'>{service.icon}</div>\n              <h3 className='text-2xl font-bold text-white mb-4'>{service.title}</h3>\n              <p className='text-gray-400'>{service.description}</p>\n            </motion.div>\n          ))}\n        </div>\n      </div>\n    </section>\n  );\n};\n\nexport default ServicesGrid;